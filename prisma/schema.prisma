generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model College {
  id         Int      @id @default(autoincrement())
  name       String   @unique
  location   String
  fees       String
  rating     Float
  placements String
  courses    String[]
  reviews    String
  type       String
}